# プロジェクト引き継ぎドキュメント

## プロジェクト概要
Google Apps Script (GAS) のローカル管理プロジェクト。Google Sheetsと連携する複数のGASスクリプトをローカルで管理・開発する。

## ディレクトリ構成
```
/home/user/-/
├── CLAUDE.md                  ← このファイル（引き継ぎドキュメント）
├── gas-project/               ← clasp対応GASプロジェクト（メイン）
│   ├── .clasp.json            ← claspプロジェクト設定（スクリプトIDは未設定）
│   ├── appsscript.json        ← GASマニフェスト（Asia/Tokyo, V8）
│   └── fillCSProductivity.gs  ← CS生産性タブ自動記入スクリプト（v16, 730行）
├── 生産性タブ/
│   └── fillCSProductivity.gs  ← 同スクリプトの元ファイル（gas-projectにコピー済み）
└── クラススタート通知/
    └── クラス案内進捗管理シート.gs  ← クラス案内関連スクリプト
```

## 完了済みの作業

### CS生産性タブスクリプト（fillCSProductivity.gs）
v11 → v16 まで段階的に開発済み。主な機能：

1. **メンバー別タスクマッチング（v12）**: CS生産性タブのB列メンバー名に対し、14人分のエイリアスで「全タスクの洗い出し」「イベント運営_CTO用」タブから該当タスクを類似度マッチングで特定
2. **頻度キャップ付きL列計算（v14）**: H列の頻度に応じてL列（見積時間）にキャップを適用
3. **追加頻度タイプ対応（v15）**: 3ヶ月1回, 開催1/2週前, 月初, 月末, 仕組み化, 単発
4. **月別サマリー出力（v16）**: A列の月情報で月ごとにグループ化し、メンバー別K列・L列合計を月別+全体で出力

### 頻度キャップルール（cspGetFrequencyCap関数）
| 頻度 | キャップ値 | 動作 |
|------|-----------|------|
| 毎日, 随時, 不定期, 都度, 空欄 | -1（無制限） | 各行 = I列時間そのまま |
| 開催1週前：単発, 開催2週前：単発 | -1（無制限） | 同上 |
| 隔週 | 4 | 4回まではそのまま、超過分は按分 |
| 週N | N | N回まではそのまま、超過分は按分 |
| 月N（月初・月末除く） | N | 同上 |
| 年N | N | 同上 |
| 3ヶ月1回, 月初, 月末, 仕組み化, 単発 | 1 | 1回ならそのまま、2回以上なら1回分に按分 |

### L列計算ロジック（Pass 3）
```javascript
if (cap === -1) {
  colL = rawMinutes; // そのまま
} else if (occurrences <= cap) {
  colL = rawMinutes; // キャップ内なのでそのまま
} else {
  colL = (rawMinutes * cap) / occurrences; // 超過分を按分
}
```

### 14人のメンバーとエイリアス（CSP_MEMBER_ALIASES）
スクリプト内に定義済み。3人は兼業メンバー（小林未侑, 中村八重子, 松元陸）で、「全タスクの洗い出し」と「イベント運営_CTO用」の両方を検索。他の11人は「全タスクの洗い出し」のみ。

### 類似度マッチング（cspCalculateSimilarity）
Jaccard類似度 + bigram類似度 + ドメインキーワードボーナス + カテゴリボーナスの4要素で総合スコアを算出。

### 月別サマリー構造
```
【12月】 | | | 合計区分    | K列合計 | L列合計
メンバーA | | | メンバーA 合計 | Xh    | Yh
メンバーB | | | メンバーB 合計 | Xh    | Yh
         | | | 12月 全体合計  | Xh    | Yh

【全体】 | | | 合計区分    | K列合計 | L列合計
メンバーA | | | メンバーA 合計 | Xh    | Yh
         | | | 全体合計     | Xh    | Yh
```

## 未完了の作業（次のチャットで対応）

### Web上のGASプロジェクトをローカルに保存
以下3つのプロジェクトをローカルに登録する必要がある：
1. **講師と生徒のスケジューリング2**
2. **SnsClubオンラインイベントカレンダー**
3. **1on1日程調整を講師へ連絡**

対応方法：
- ユーザーがWeb上のApps Scriptエディタからコードをコピーしてチャットに貼り付け
- それをローカルの適切なディレクトリに保存
- 各プロジェクトごとにclasp設定（.clasp.json, appsscript.json）を作成
- Git管理下に追加

### clasp連携（任意）
- `.clasp.json` のスクリプトIDはまだ `YOUR_SCRIPT_ID_HERE` のまま
- 実際に `clasp push/pull` で同期するには、各プロジェクトのスクリプトIDを設定する必要がある
- スクリプトIDはApps Scriptエディタの「プロジェクトの設定」から取得

## 技術メモ
- ランタイム: V8
- タイムゾーン: Asia/Tokyo
- clasp: グローバルインストール済み（@google/clasp）
- ユーザーの言語: 日本語で対応すること
- コード共有時: ファイルに書くだけでなく、チャット上にもコードブロックで表示すること（ユーザーの過去の要望）
